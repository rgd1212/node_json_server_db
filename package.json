{
  "name": "3.-json-server",
  "version": "0.0.1",
  "description": "Armar un pequeño servidor con informacion del curso",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json",
    "dev": "json-server --watch db.json"
  },
  "author": "rodrigorgd",
  "license": "ISC",
  "dependencies": {
    "json-server": "^1.0.0-beta.2"
  }
}
